import winsound

# Frequency and duration for the beep (you can adjust these values)
frequency = 1000  # Frequency in Hertz (e.g., 1000 Hz)
duration = 1000  # Duration in milliseconds (e.g., 1000 ms or 1 second)

# Produce a beep sound
winsound.Beep(frequency, duration)
